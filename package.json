{
  "name": "skill2cash",
  "version": "1.0.0",
  "private": true,
  "description": "SKILL2CASH full-stack V1 for eFootball money duels.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "npm run dev:memory",
    "dev:memory": "set PORT=5001&& set VITE_API_URL=http://localhost:5001/api&& concurrently \"npm run dev:memory --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:mongo": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run start --prefix backend",
    "start:memory": "npm run start:memory --prefix backend",
    "seed": "npm run seed --prefix backend",
    "seed:memory": "npm run seed:memory --prefix backend",
    "test": "npm run test --prefix backend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
